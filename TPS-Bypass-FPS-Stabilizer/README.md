# TPS Bypass & FPS Stabilizer

Experimental Geode mod for Geometry Dash. See about.md for details.

## Building
Requires the Geode SDK (GEODE_SDK env var set). Standard Geode CMake build:

```
cmake -B build
cmake --build build
```

